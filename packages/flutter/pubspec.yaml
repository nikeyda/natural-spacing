name: natural_spacing_flutter
description: Experimental Flutter TextInputFormatter for Natural Spacing.
version: 0.0.0
publish_to: none

environment:
  sdk: '>=3.13.0 <4.0.0'
  flutter: '>=3.47.0'

dependencies:
  flutter:
    sdk: flutter
  natural_spacing:
    path: ../dart

dev_dependencies:
  flutter_test:
    sdk: flutter
